# from django.url import path
# from . import views
#
# urlpatterns = [ path('users/login/', views.MyTokenObtainPairView.as_view(), name='token_obtain_pair'),
# we use access token to authorize
#
#     path('users/register/', views.register_user, name='register_user'),
#
#     path('users/profile/', views.get_user_profile, name='get_user_profile'),
#     path('users/', views.get_users, name='get_users'),
#
#     path('products/', views.get_products, name="get_products"),
#     path('products/<str:pk>', views.get_product, name="get_product"),
# ]

from django.url import path
from . import views

urlpatterns = [

]
